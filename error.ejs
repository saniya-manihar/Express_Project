<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - No Posts Available</title>
    <style>
        /* General body styling */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #333;
        }

        /* Error message container */
        .error-container {
            text-align: center;
            background-color: #fff;
            border: 2px solid #f5c6cb;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            margin: 0 auto;
        }

        /* Error message text */
        .error-container h3 {
            color: #e74c3c; /* Red color for error message */
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        /* Emoji styling */
        .error-container .emoji {
            font-size: 50px; /* Larger emoji size */
            margin-bottom: 10px;
        }

        /* Additional hint or error description */
        .error-container p {
            color: #888;
            font-size: 14px;
        }

    </style>
</head>
<body>

    <!-- Error message container -->
    <div class="error-container">
        <div class="emoji">⚠️❌</div>
        <h3>No Post is available</h3>
        <p>It looks like there are no posts at the moment. Please check back later!</p>
    </div>

</body>
</html>
